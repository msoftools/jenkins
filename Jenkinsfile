
pipeline {
    agent any

    stages {
        stage('1') {
            steps {
                sh '/tmp/hello.sh'
            }
        }
    }

    stages {
        stage('2') {
            steps {
                sh '/tmp/hello.sh'
            }
        }
    }
}
